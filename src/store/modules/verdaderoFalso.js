/*
import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    pruebaFinal: [],
    detallePrueba: {},
  },
  getters: {},
  mutations: {
    agregarDetallePrueba(state) {
      state.pruebaFinal = [state.detallePrueba, ...state.pruebaFinal];
    },
  },
  actions: {
    agregarDetallePruebaAction(context) {
      context.commit("agregarDetallePrueba");
    },
  }, // para manipular los statos y trabajan en paralelo con las mutaciones
  modules: {},
});
*/
